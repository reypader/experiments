---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: kubernetes-informer-coordination-demo
  namespace: default
  labels:
    app: kubernetes-informer-coordination-demo
spec:
  replicas: 2  # Deploy multiple instances to see cluster membership
  selector:
    matchLabels:
      app: kubernetes-informer-coordination-demo
  template:
    metadata:
      labels:
        app: kubernetes-informer-coordination-demo
    spec:
      serviceAccountName: kubernetes-informer-coordination-demo
      containers:
        - name: app
          image: kubernetes-informer-coordination-demo:latest
          imagePullPolicy: Never  # For Minikube local images
          env:
            - name: POD_NAME
              valueFrom:
                fieldRef:
                  fieldPath: metadata.name
            - name: NAMESPACE
              valueFrom:
                fieldRef:
                  fieldPath: metadata.namespace
          resources:
            requests:
              memory: "256Mi"
              cpu: "100m"
            limits:
              memory: "512Mi"
              cpu: "500m"